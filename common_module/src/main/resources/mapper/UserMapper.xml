<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sb.project.common_module.repository.mybatis.UserMapper">
    <select id="findById" parameterType="long" resultType="sb.project.common_module.entity.User">
        SELECT * FROM users WHERE id = #{id}
    </select>
</mapper>